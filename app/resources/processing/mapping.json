{
  "_editing_guide": {
    "safe_to_edit": [
      "field_mapping values (right side source column names)",
      "content_hints.sample_size",
      "content_hints.column_value_patterns"
    ],
    "keep_stable": [
      "field_mapping keys (left side canonical target fields)",
      "category name in upload_types.json if already used in historical uploads"
    ],
    "notes": [
      "Mapping direction is target_field -> source_csv_column.",
      "If you do not need date-based split logic for fact files, keep using file_type=dim so splitting is bypassed.",
      "When adding new target fields, also add matching content_hints rules for better auto-detection confidence.",
      "Use separate JSON files under app/resources/processing/mapping_types for filename-based output rules."
    ]
  },
  "_comment_field_mapping": "Dummy/editable mapping category for dim lookup tables. Keep target_field -> source_csv_column.",
  "storage_options": {
    "path_mode": "category_only",
    "use_original_filename": true,
    "default_output_filename": "mapping_default.csv"
  },
  "field_mapping": {
    "category": "category",
    "mapping_type": "mapping_type",
    "source_value": "source_value",
    "mapped_value": "mapped_value",
    "priority": "priority",
    "is_active": "is_active",
    "effective_date": "effective_date",
    "notes": "notes"
  },
  "content_hints": {
    "sample_size": 50,
    "column_value_patterns": [
      {
        "column": "category",
        "pattern": "^.+$",
        "weight": 0.8
      },
      {
        "column": "source_value",
        "pattern": "^.+$",
        "weight": 1.0
      },
      {
        "column": "mapped_value",
        "pattern": "^.+$",
        "weight": 1.0
      },
      {
        "column": "priority",
        "pattern": "^\\d*$",
        "weight": 0.7
      },
      {
        "column": "is_active",
        "pattern": "^(?i:true|false|1|0|yes|no)$",
        "weight": 0.7
      }
    ]
  }
}
